#include <iostream>
#include<math.h>
using namespace std;
int main() {
	int n,i,a[90];
  int sum;
  cin>>n;
  for(i=0;i<n;i++)
  {
    cin>>a[i];
    sum=sqrt(a[i]);
    cout<<sum<<endl;
  }
  
	return 0;
}
