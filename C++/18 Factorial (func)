#include <iostream>
using namespace std;
inline void friends(int x,int y)
{
  cout<<"Factorial of "<<x<<" is "<<y;
}
int main() 
{
  int a,b,i,sum=1;
  cin>>a;
  for(i=1;i<=a;i++)
  {
    sum=sum*i;
    
  }
    friends(a,sum);
  
	return 0;
}
